import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
	/* config options here */
	images: {
		domains: ['bialecki.pl', 'gunter.pl', 'lh6.googleusercontent.com', 'cdn.pixabay.com'],
	},
}

export default nextConfig
